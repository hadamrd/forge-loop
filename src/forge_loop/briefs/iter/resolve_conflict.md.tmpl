You are resuming work on issue #{issue_n}: {title}

PR {pr_url} has merge CONFLICTS against `{base_branch}`. Your ONLY job is to resolve them.

1. `cd {worktree}`.
2. `git fetch origin {base_branch}`.
3. `git merge origin/{base_branch}` (or `git rebase origin/{base_branch}` if the branch is clean).
4. Resolve conflicts. Run the affected tests.
5. `git add -A && git commit` (or `git rebase --continue`).
6. `git push origin {branch}` (force-with-lease if rebased).

If conflicts are genuinely irresolvable without product input, exit with status="failed"
and write a short summary to {worktree}/CONFLICT_NOTES.md.

Then exit. Attempt {attempt} of {max_attempts}.

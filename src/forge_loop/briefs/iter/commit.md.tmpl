You are resuming work on issue #{issue_n}: {title}

Prior worker session exited DIRTY: files were edited but `git commit` was never run.

Your ONLY job is to ship what is already on disk. Do NOT explore. Do NOT add features.

1. `cd {worktree}` (already on branch `{branch}`).
2. `git status --short` — review the dirty files.
3. `git add -A && git commit -m "..."` referencing #{issue_n} with a body explaining why.
   Sign as: `Co-Authored-By: {coauthor}`
4. `git push -u origin {branch}`.
5. `gh pr create --base {base_branch} --title "..." --body "Closes #{issue_n}..."` if no PR exists.
6. `gh pr merge <N> --squash --auto --delete-branch`.

Then exit. Attempt {attempt} of {max_attempts}.

You are resuming work on issue #{issue_n}: {title}

Prior worker session exited but produced NOTHING — no commits, no PR, clean worktree.

Issue body:
{body}

Your ONLY job is to ship this issue end-to-end:

1. `cd /tmp/wt-loop-{issue_n}`.
2. Implement the acceptance criteria from the issue body.
3. Add tests (happy path + adversarial).
4. `git add -A && git commit` (with a body referencing #{issue_n}).
5. `git push -u origin {branch}`.
6. `gh pr create --base {base_branch}` then `gh pr merge <N> --squash --auto --delete-branch`.

Attempt {attempt} of {max_attempts}. Do NOT explore for an hour — make decisions and ship.

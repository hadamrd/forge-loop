You are resuming work on issue #{issue_n}: {title}

Branch `{branch}` is pushed to origin but no PR exists.

Your ONLY job is to open the PR. Do NOT edit any code.

1. `cd /tmp/wt-loop-{issue_n}`.
2. `gh pr create --base {base_branch} --head {branch} --title "..." --body "Closes #{issue_n}. <restate acceptance criteria>"`.
3. `gh pr merge <N> --squash --auto --delete-branch`.

Then exit. Attempt {attempt} of {max_attempts}.

"""Built-in role definitions shipped with forge-loop (#17).

Each ``*.yaml`` file in this package is a default role. Operators override
any of them by dropping a same-named YAML into ``<project>/.forge/roles/``.
"""

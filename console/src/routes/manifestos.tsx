// PLACEHOLDER — replaced by the ported screen. Default export consumed by src/router.tsx.
export default function ManifestosScreen() {
  return (
    <div className="page">
      <div className="empty">
        <div className="empty-ic" style={{ color: "var(--amber)", background: "color-mix(in oklab,var(--amber) 12%,transparent)" }} />
        <div style={{ color: "var(--hi)", fontWeight: 600, fontSize: 13.5 }}>Manifestos — wiring up</div>
        <div style={{ color: "var(--dim)", fontSize: 12.5 }}>This screen mounts against the mock ForgeApi.</div>
      </div>
    </div>
  );
}
